# Python 3.14.7 -- pinned to the versions the reported results were run on.
# Install:  python -m venv .venv && .venv/bin/pip install -r requirements.txt
mne==1.12.1
numpy==2.5.3
scipy==1.18.1
torch==2.14.0
scikit-learn==1.9.0
snntorch==1.0.0
